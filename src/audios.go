package main

/* const (
	sampleRate = 44100
)

type audioPlayers struct {
	audioPlayerJump *audio.Player
	audioContext    *audio.Context
}

func NewAudioContext() *audio.Context {
	audioContext := audio.NewContext(sampleRate)

	return audioContext
}

// Opens, decodes and loads audio on specified path into an audioplayer
func LoadWavAudio(path string, audioContext *audio.Context) *audio.Player {

	file, err := ebitenutil.OpenFile(path)
	if err != nil {
		log.Fatal(err)
	}
	decoded, err := wav.Decode(audioContext, file)
	audioPlayer, err := audio.NewPlayer(audioContext, decoded)

	return audioPlayer
}
*/
